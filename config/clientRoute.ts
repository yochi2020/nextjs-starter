export const rootPath = {
  propertyContent: "",
  rateAndAvailability: "",
};

const clientRoute = {
  dashboard: "dashboard",
  auth: {
    login: "/auth/login",
  },
};

export default clientRoute;
